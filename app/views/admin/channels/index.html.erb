<h1 class="page-header">频道管理<small> 主页显示项目</small></h1>

<ul class="link-group">
  <% if can? :manage, Article%>
  <li>
    <%= link_to "文章管理", admin_articles_path %>
  </li>
  <% end %>
</ul>

<%= link_to new_admin_channel_path,  class: "btn btn-default" do %>
<span class="glyphicon glyphicon-plus"></span> 新建
<% end %>

<%= alert_info %>

<table class="table">
  <thead>
    <tr>
      <th>#</th>
      <th>名称</th>
      <th>链接</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <% @channels.each_with_index do |channel, index| %>
    <tr>
      <td><%= index+1 %></td>
      <td><%= channel.name %></td>
      <td><%= channel.url%></td>
      <td>
        <%= link_to admin_channel_path(channel) do %>
        <span class="glyphicon glyphicon-eye-open icon"></span>
        <% end %>
        <%= link_to edit_admin_channel_path(channel) do %>
        <span class="glyphicon glyphicon-pencil icon"></span>
        <% end %>
        <%= link_to admin_channel_path(channel), method: :delete, data: {confirm: %(确定要删除"#{channel.name}"吗？)} do %>
        <span class="glyphicon glyphicon-trash icon"></span>
        <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
  <tfoot>
    <tr>
      <td colspan="6">
        <%= page_info @channels %>
        <%= will_paginate @channels, page_links: false %>
      </td>
    </tr>
    </tfoot>
</table>
