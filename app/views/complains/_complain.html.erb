<div class="title"><%=display_date complain.created_at%></div>
<div class="content">
  <% if complain.order.present? %>
  <p>订单号：<%= complain.order.order_id %></p>
  <% end %>
  <p><%=complain.content%></p>
  <small>
    <span class="pull-left">处理结果： <%= complain_status complain%></span>
    <span class="pull-right">处理人： <%= complain_staff complain.staff %></span>
  </small>
</div>
